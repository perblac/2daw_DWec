{
  "name": "prj-pokeapi",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run fetch-and-save && start /b npm run pokeapi && vite",
    "build": "vite build",
    "preview": "vite preview",
    "pokeapi": "json-server --watch server/db.json --port 4000",
    "fetch-and-save": "node src/helpers/fetchAndSave.js"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "node-fetch": "^3.3.2",
    "vite": "^4.4.5"
  }
}
